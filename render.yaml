services:
  - type: web
    name: university-participation-system
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: ADMIN_KEY
        sync: false
      - key: NODE_VERSION
        value: 22.5.0
